add_library(engine_time STATIC)
target_sources(engine_time PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
	simulation_time.cppm simulation_clock.cppm)
target_compile_features(engine_time PUBLIC cxx_std_23)
set_property(TARGET engine_time PROPERTY CXX_SCAN_FOR_MODULES ON)
if(TARGET core_config)
	target_link_libraries(engine_time PUBLIC core_config)
endif()

if(ENGINE_TIME_BUILD_TESTS)
	find_package(boost_included_unit_test_framework CONFIG REQUIRED)
	set(time_tests simulation_time.test simulation_clock.test)
	set_source_files_properties(${time_tests} PROPERTIES LANGUAGE CXX)
	add_executable(engine_time_tests ${time_tests})
	set_property(TARGET engine_time_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_time_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_time_tests PRIVATE engine_time Boost::included_unit_test_framework)
	enable_testing()
	add_test(NAME engine_time_tests COMMAND engine_time_tests)
endif()
