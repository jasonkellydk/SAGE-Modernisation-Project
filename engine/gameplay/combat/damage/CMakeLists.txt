add_library(engine_gameplay_damage STATIC)
target_sources(engine_gameplay_damage PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    components/damage_packet.cppm definitions/armor_catalog.cppm algorithms/resolve_damage.cppm inputs/accepted_hit_batch.cppm)
target_compile_features(engine_gameplay_damage PUBLIC cxx_std_23)
target_link_libraries(engine_gameplay_damage PUBLIC engine_ecs)
if(ENGINE_GAMEPLAY_BUILD_TESTS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    set(damage_tests components/damage_packet.test definitions/armor_catalog.test algorithms/resolve_damage.test inputs/accepted_hit_batch.test)
    set_source_files_properties(${damage_tests} PROPERTIES LANGUAGE CXX)
    add_executable(engine_gameplay_damage_tests ${damage_tests})
    target_compile_definitions(engine_gameplay_damage_tests PRIVATE BOOST_TEST_INCLUDED)
    target_link_libraries(engine_gameplay_damage_tests PRIVATE engine_gameplay_damage Boost::included_unit_test_framework)
    add_test(NAME engine_gameplay_damage_tests COMMAND engine_gameplay_damage_tests)
endif()
