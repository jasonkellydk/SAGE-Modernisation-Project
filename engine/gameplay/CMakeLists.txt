cmake_minimum_required(VERSION 4.4.3)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
	project(engine_gameplay_check LANGUAGES CXX)
endif()

option(ENGINE_GAMEPLAY_BUILD_TESTS "Build headless gameplay tests" OFF)
if(NOT TARGET engine_ecs)
	add_subdirectory("${CMAKE_CURRENT_SOURCE_DIR}/../ecs" "${CMAKE_CURRENT_BINARY_DIR}/engine_ecs")
endif()
if(ENGINE_GAMEPLAY_BUILD_TESTS)
	enable_testing()
endif()
add_subdirectory(lifetime)
add_subdirectory(containment)
add_subdirectory(spatial)
add_subdirectory(combat)
add_subdirectory(navigation)
add_subdirectory(spatial/contacts)
add_subdirectory(pickups)
add_subdirectory(concealment)
add_subdirectory(progression)
add_subdirectory(rts)
