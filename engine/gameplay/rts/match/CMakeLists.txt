add_library(engine_gameplay_rts_match STATIC)
target_sources(engine_gameplay_rts_match PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES systems/elimination_system.cppm components/contender.cppm)
target_compile_features(engine_gameplay_rts_match PUBLIC cxx_std_23)
target_link_libraries(engine_gameplay_rts_match PUBLIC engine_ecs)
if(ENGINE_GAMEPLAY_BUILD_TESTS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    set_source_files_properties(systems/elimination_system.test components/contender.test PROPERTIES LANGUAGE CXX)
    add_executable(engine_gameplay_match_tests systems/elimination_system.test components/contender.test)
    target_compile_definitions(engine_gameplay_match_tests PRIVATE BOOST_TEST_INCLUDED)
    target_link_libraries(engine_gameplay_match_tests PRIVATE engine_gameplay_rts_match Boost::included_unit_test_framework)
    add_test(NAME engine_gameplay_match_tests COMMAND engine_gameplay_match_tests)
endif()
