add_library(engine_gameplay_rts_radar STATIC)
target_sources(engine_gameplay_rts_radar PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
	components/radar_availability.cppm components/radar_batch_range.cppm
	algorithms/radar_availability.cppm availability/radar_availability.cppm
	commands/radar_batch.cppm systems/radar_system.cppm)
target_compile_features(engine_gameplay_rts_radar PUBLIC cxx_std_23)
set_property(TARGET engine_gameplay_rts_radar PROPERTY CXX_SCAN_FOR_MODULES ON)
target_link_libraries(engine_gameplay_rts_radar PUBLIC engine_ecs)
if(ENGINE_GAMEPLAY_BUILD_TESTS)
	find_package(boost_included_unit_test_framework CONFIG REQUIRED)
	set_source_files_properties(algorithms/radar_availability.test PROPERTIES LANGUAGE CXX)
	add_executable(engine_gameplay_rts_radar_tests algorithms/radar_availability.test)
	set_property(TARGET engine_gameplay_rts_radar_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_gameplay_rts_radar_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_gameplay_rts_radar_tests PRIVATE engine_gameplay_rts_radar Boost::included_unit_test_framework)
	add_test(NAME engine_gameplay_rts_radar_tests COMMAND engine_gameplay_rts_radar_tests)
	set_source_files_properties(components/radar_availability.test PROPERTIES LANGUAGE CXX)
	add_executable(engine_gameplay_rts_radar_availability_component_tests components/radar_availability.test)
	set_property(TARGET engine_gameplay_rts_radar_availability_component_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_gameplay_rts_radar_availability_component_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_gameplay_rts_radar_availability_component_tests PRIVATE engine_gameplay_rts_radar Boost::included_unit_test_framework)
	add_test(NAME engine_gameplay_rts_radar_availability_component_tests COMMAND engine_gameplay_rts_radar_availability_component_tests)
	set_source_files_properties(components/radar_batch_range.test PROPERTIES LANGUAGE CXX)
	add_executable(engine_gameplay_rts_radar_batch_range_tests components/radar_batch_range.test)
	set_property(TARGET engine_gameplay_rts_radar_batch_range_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_gameplay_rts_radar_batch_range_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_gameplay_rts_radar_batch_range_tests PRIVATE engine_gameplay_rts_radar Boost::included_unit_test_framework)
	add_test(NAME engine_gameplay_rts_radar_batch_range_tests COMMAND engine_gameplay_rts_radar_batch_range_tests)
	foreach(radar_test commands/radar_batch systems/radar_system)
		get_filename_component(radar_stem "${radar_test}" NAME)
		set_source_files_properties("${radar_test}.test" PROPERTIES LANGUAGE CXX)
		add_executable(engine_${radar_stem}_tests "${radar_test}.test")
		set_property(TARGET engine_${radar_stem}_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
		target_link_libraries(engine_${radar_stem}_tests PRIVATE engine_gameplay_rts_radar Boost::included_unit_test_framework)
		add_test(NAME engine_${radar_stem}_tests COMMAND engine_${radar_stem}_tests)
	endforeach()
endif()
