add_library(engine_gameplay_rts_economy STATIC)
target_sources(engine_gameplay_rts_economy PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
	components/periodic_income.cppm components/resource_balance.cppm components/income_dispatch.cppm systems/income_system.cppm)
target_compile_features(engine_gameplay_rts_economy PUBLIC cxx_std_23)
set_property(TARGET engine_gameplay_rts_economy PROPERTY CXX_SCAN_FOR_MODULES ON)
target_link_libraries(engine_gameplay_rts_economy PUBLIC engine_ecs)
if(ENGINE_GAMEPLAY_BUILD_TESTS)
	find_package(boost_included_unit_test_framework CONFIG REQUIRED)
	set(economy_tests components/periodic_income.test components/resource_balance.test components/income_dispatch.test systems/income_system.test)
	set_source_files_properties(${economy_tests} PROPERTIES LANGUAGE CXX)
	add_executable(engine_gameplay_rts_economy_tests ${economy_tests})
	set_property(TARGET engine_gameplay_rts_economy_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_gameplay_rts_economy_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_gameplay_rts_economy_tests PRIVATE engine_gameplay_rts_economy Boost::included_unit_test_framework)
	add_test(NAME engine_gameplay_rts_economy_tests COMMAND engine_gameplay_rts_economy_tests)
endif()
