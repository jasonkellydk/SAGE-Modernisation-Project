add_library(engine_gameplay_spatial STATIC)
target_sources(engine_gameplay_spatial PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    grid/point_grid.cppm algorithms/grid_radius.cppm)
target_compile_features(engine_gameplay_spatial PUBLIC cxx_std_23)
target_link_libraries(engine_gameplay_spatial PUBLIC engine_ecs)
if(ENGINE_GAMEPLAY_BUILD_TESTS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    set_source_files_properties(grid/point_grid.test algorithms/grid_radius.test PROPERTIES LANGUAGE CXX)
    add_executable(engine_gameplay_spatial_tests grid/point_grid.test algorithms/grid_radius.test)
    target_compile_definitions(engine_gameplay_spatial_tests PRIVATE BOOST_TEST_INCLUDED)
    target_link_libraries(engine_gameplay_spatial_tests PRIVATE engine_gameplay_spatial Boost::included_unit_test_framework)
    add_test(NAME engine_gameplay_spatial_tests COMMAND engine_gameplay_spatial_tests)
endif()
