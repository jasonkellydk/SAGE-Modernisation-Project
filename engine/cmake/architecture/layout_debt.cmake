# Existing layout violations, not architectural acceptance or a growth allowance.
# Remove entries as corrected. New files fail the tree-wide guard; fixing only
# the user's named example is not completion. See migration/parallel_port.md.
set(gameplay_component_layout_debt)
set(gameplay_system_layout_debt)
