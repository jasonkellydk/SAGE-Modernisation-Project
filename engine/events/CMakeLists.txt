add_library(engine_events STATIC)
target_sources(engine_events PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
	ordering/batch_order.cppm storage/event_batch.cppm schema/message_registry.cppm)
target_compile_features(engine_events PUBLIC cxx_std_23)
set_property(TARGET engine_events PROPERTY CXX_SCAN_FOR_MODULES ON)
if(TARGET core_config)
	target_link_libraries(engine_events PUBLIC core_config)
endif()

if(ENGINE_EVENTS_BUILD_TESTS)
	find_package(boost_included_unit_test_framework CONFIG REQUIRED)
	set(event_tests ordering/batch_order.test storage/event_batch.test schema/message_registry.test)
	set_source_files_properties(${event_tests} PROPERTIES LANGUAGE CXX)
	add_executable(engine_events_tests ${event_tests})
	set_property(TARGET engine_events_tests PROPERTY CXX_SCAN_FOR_MODULES ON)
	target_compile_definitions(engine_events_tests PRIVATE BOOST_TEST_INCLUDED)
	target_link_libraries(engine_events_tests PRIVATE engine_events engine_jobs Boost::included_unit_test_framework)
	enable_testing()
	add_test(NAME engine_events_tests COMMAND engine_events_tests)
endif()
