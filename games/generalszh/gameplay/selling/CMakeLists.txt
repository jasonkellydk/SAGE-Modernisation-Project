add_library(generalszh_sale_components STATIC)
target_sources(generalszh_sale_components PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    components/sale_state.cppm)
target_compile_features(generalszh_sale_components PUBLIC cxx_std_23)
target_link_libraries(generalszh_sale_components PUBLIC engine_ecs)

add_library(generalszh_selling STATIC)
target_sources(generalszh_selling PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    definitions/sell_definition.cppm inputs/sell_batch.cppm systems/sell_system.cppm)
target_compile_features(generalszh_selling PUBLIC cxx_std_23)
target_link_libraries(generalszh_selling PUBLIC generalszh_sale_components generalszh_construction_definitions generalszh_build_execution generalszh_economy_rules)
if(RTS_BUILD_ZEROHOUR_EXTRAS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    foreach(selling_test IN ITEMS definitions/sell_definition components/sale_state inputs/sell_batch systems/sell_system)
        get_filename_component(selling_stem "${selling_test}" NAME)
        set_source_files_properties("${selling_test}.test" PROPERTIES LANGUAGE CXX)
        add_executable("generalszh_${selling_stem}_tests" "${selling_test}.test")
        if(selling_stem STREQUAL "sale_state")
            target_link_libraries("generalszh_${selling_stem}_tests" PRIVATE generalszh_sale_components Boost::included_unit_test_framework)
        else()
            target_link_libraries("generalszh_${selling_stem}_tests" PRIVATE generalszh_selling Boost::included_unit_test_framework)
        endif()
        add_test(NAME "generalszh_${selling_stem}_tests" COMMAND "generalszh_${selling_stem}_tests")
    endforeach()
    target_link_libraries(generalszh_sell_system_tests PRIVATE generalszh_production_execution)
endif()
