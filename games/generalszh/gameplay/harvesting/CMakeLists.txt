add_library(generalszh_harvesting_execution STATIC)
target_sources(generalszh_harvesting_execution PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    components/supply_dropoff_owner.cppm revenue/harvest_revenue.cppm)
target_compile_features(generalszh_harvesting_execution PUBLIC cxx_std_23)
target_link_libraries(generalszh_harvesting_execution PUBLIC engine_gameplay_rts_harvesting generalszh_build_execution generalszh_account_execution)
if(RTS_BUILD_ZEROHOUR_EXTRAS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    set(harvest_tests components/supply_dropoff_owner.test revenue/harvest_revenue.test)
    set_source_files_properties(${harvest_tests} PROPERTIES LANGUAGE CXX)
    add_executable(generalszh_harvesting_tests ${harvest_tests})
    target_compile_definitions(generalszh_harvesting_tests PRIVATE BOOST_TEST_INCLUDED)
    target_link_libraries(generalszh_harvesting_tests PRIVATE generalszh_harvesting_execution Boost::included_unit_test_framework)
    add_test(NAME generalszh_harvesting_tests COMMAND generalszh_harvesting_tests)
endif()
