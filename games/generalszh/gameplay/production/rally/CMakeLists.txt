add_library(generalszh_rally_components STATIC)
target_sources(generalszh_rally_components PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES components/rally_point.cppm)
target_compile_features(generalszh_rally_components PUBLIC cxx_std_23)
target_link_libraries(generalszh_rally_components PUBLIC engine_gameplay_rts_orders)

add_library(generalszh_production_rally STATIC)
target_sources(generalszh_production_rally PUBLIC FILE_SET cxx_modules TYPE CXX_MODULES FILES
    inputs/rally_batch.cppm systems/rally_system.cppm)
target_compile_features(generalszh_production_rally PUBLIC cxx_std_23)
target_link_libraries(generalszh_production_rally PUBLIC generalszh_rally_components generalszh_build_execution engine_gameplay_navigation)
if(RTS_BUILD_ZEROHOUR_EXTRAS)
    find_package(boost_included_unit_test_framework CONFIG REQUIRED)
    foreach(rally_test IN ITEMS components/rally_point inputs/rally_batch systems/rally_system)
        get_filename_component(rally_stem "${rally_test}" NAME)
        set_source_files_properties("${rally_test}.test" PROPERTIES LANGUAGE CXX)
        add_executable("generalszh_${rally_stem}_tests" "${rally_test}.test")
        if(rally_stem STREQUAL "rally_point")
            target_link_libraries("generalszh_${rally_stem}_tests" PRIVATE generalszh_rally_components Boost::included_unit_test_framework)
        else()
            target_link_libraries("generalszh_${rally_stem}_tests" PRIVATE generalszh_production_rally Boost::included_unit_test_framework)
        endif()
        add_test(NAME "generalszh_${rally_stem}_tests" COMMAND "generalszh_${rally_stem}_tests")
    endforeach()
    target_link_libraries(generalszh_rally_system_tests PRIVATE generalszh_production_execution generalszh_game_session)
endif()
